application:
  host: 0.0.0.0
  Port: 5000
  mode: debug # debug / prod
  read_timeout: 5
  write_timeout: 5
  session_secret: 
  session_domain: 
  allow_service:
    - ".*hustunique.com.*"

database:
  postgres_dsn: host= user= password= dbname= port= sslmode= TimeZone=Asia/Shanghai

redis:
  addr:
  password:
  db:

sms:
  secret_id:
  secret_key:
  app_id:

lark:
  app_id:
  app_secret:
  redirect_uri:
